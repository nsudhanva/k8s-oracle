apiVersion: apps/v1
kind: Deployment
metadata:
  name: lakshmi-client
  namespace: lakshmi
  labels:
    app: lakshmi-client
spec:
  replicas: 2
  selector:
    matchLabels:
      app: lakshmi-client
  template:
    metadata:
      labels:
        app: lakshmi-client
    spec:
      imagePullSecrets:
        - name: regcred
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - lakshmi-client
                topologyKey: kubernetes.io/hostname
      containers:
        - name: client
          image: ghcr.io/nsudhanva/lakshmi-client:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
              name: http
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: lakshmi-client
  namespace: lakshmi
  labels:
    app: lakshmi-client
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      name: http
  selector:
    app: lakshmi-client
